# Backend environment example for BloodLine
# Copy to .env and replace placeholders before running

SUPABASE_URL=https://your-supabase-project.supabase.co
SUPABASE_SERVICE_KEY=your-supabase-service-role-key-REPLACE_ME
PORT=5000
SEED_DEFAULT_USERS=false

# IMPORTANT: The SUPABASE_SERVICE_KEY must be the "Service Role" key
# (found in Supabase Dashboard → Settings → API). Do NOT use the
# client/public key (anon/public) for admin operations.

# Default local dev accounts (seeded automatically when server starts if the `profiles` table exists):
# admin@bloodline.local / AdminPass123!
# donor@bloodline.local / DonorPass123!
# hospital@bloodline.local / HospitalPass123!
