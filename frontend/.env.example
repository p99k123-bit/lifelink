# Copy this file to `frontend/.env.local` before running the frontend.

# Required for Supabase auth (login/signup, middleware, protected routes)
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# Optional server fallback used by server-side helpers
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_ANON_KEY=your-supabase-anon-key

# Optional backend API base URL for non-Supabase endpoints
NEXT_PUBLIC_API_URL=http://localhost:5000
